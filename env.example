# ==================================
# VARIÁVEIS DE AMBIENTE - EXEMPLO
# ==================================
# Instruções:
# 1. Copie este arquivo para .env na raiz do projeto
# 2. Ajuste os valores conforme seu ambiente

# Ambiente (development | production)
NODE_ENV=production

# Backend
BACKEND_PORT=3000
PORT=3000

# Frontend - Em produção usa proxy /api via Nginx
VITE_API_URL=/api

# ========================================
# PostgreSQL - PRODUÇÃO
# ========================================
# IMPORTANTE: Altere a senha antes de fazer deploy!
POSTGRES_DB=''
POSTGRES_USER=''
POSTGRES_PASSWORD='root'

# URL de conexão (gerada automaticamente no docker-compose)
# Não precisa alterar, apenas certifique-se que as variáveis acima estão corretas
DATABASE_URL=postgresql://app_user:root@db:5432/tasks_dbs

