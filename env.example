# ==================================
# VARIÁVEIS DE AMBIENTE - EXEMPLO
# ==================================
# Instruções:
# 1. Copie este arquivo para .env na raiz do projeto: cp env.example .env
# 2. Edite o arquivo .env e configure as variáveis abaixo

# Ambiente (development | production)
NODE_ENV=production

# Backend
BACKEND_PORT=3000
PORT=3000

# Frontend - Em produção usa proxy /api via Nginx
VITE_API_URL=/api

# ========================================
# PostgreSQL - PRODUÇÃO
# ========================================
# ⚠️  OBRIGATÓRIO: Configure estas variáveis!
# Use senhas fortes em produção (mínimo 16 caracteres)
# Exemplo de senha forte: openssl rand -base64 24

POSTGRES_DB=tasks_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=MUDE_PARA_SENHA_FORTE_AQUI

